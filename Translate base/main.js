let base = prompt("Enter the base")
let number = prompt("enter the number");

let numsolution = parseInt(number).toString(base);

console.log("the number " + number + " In base: " + base + ", is equal to: " + numsolution)